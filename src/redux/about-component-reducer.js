let initialState = {};

const aboutComponentReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default aboutComponentReducer;
